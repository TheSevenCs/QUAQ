import React from "react";
import { Link } from "react-router-dom";

const PageHours = () => {
  const calculateHours = async () => {};

  return (
    <div>
      <Link to="/">
        <button>HOME PAGE</button>
      </Link>
      <h1>HOURS PAGE</h1>
    </div>
  );
};

export default PageHours;
